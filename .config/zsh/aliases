# ls
alias ls='eza --color=auto --icons'
alias ll='ls -la --git --git-repos'
alias llt='ll -T'
alias lld='ll -D'

# vim
alias vim='nvim'

# git
alias gst='git status'
alias gpr='git pull -r'

# kubectl
alias k='kubectl'
alias -g JQ="| jaq -CRr '. as \$line | try (fromjson | [\"\u001b[33m\"+(.ts|fromdateiso8601|strftime(\"%Y-%m-%d %H:%M:%S\")), (if .level==\"info\" then \"\u001b[32m\"+.level elif .level==\"warn\" then \"\u001b[33m\"+.level elif .level==\"error\" then \"\u001b[31m\"+.level else \"\u001b[37m\"+.level end), \"\u001b[37m\"+.msg] | @tsv) catch \$line'"
#
# microk8s
alias mk='microk8s'
alias mke='microk8s enable'
alias mkd='microk8s disable'
alias mkst='microk8s status'
alias mkup='microk8s start'
alias mkdn='microk8s stop'
alias mkrs='microk8s reset'
alias mkvs='microk8s version'
alias mkcl='microk8s config'
alias mkk='microk8s kubectl'

# miscellaneous
alias vc='vim ~/.zshrc'
alias sa='source ~/.zshrc'
alias stowd='pushd ~/dotfiles && stow . && popd'

# python
# alias python='/opt/homebrew/bin/python3'
# alias pip='/opt/homebrew/bin/pip3'

alias tma='tmux attach'


# alias start='export KUBECONFIG=~/.kube/core-services-dev && export AWS_PROFILE=DevOpsAdmin && aws sso login --sso-session DevOpsAdmin'
alias tfp='terraform plan -var-file="env_settings/dev.tfvars"'
alias tfa='terraform apply -var-file="env_settings/dev.tfvars"'
alias tfd='terraform destroy -var-file="env_settings/dev.tfvars"'

# alias <cmd>='<statement>'
# alias -s <ext>=<action> -- just type the file name and it executes action based on ext
# alias -g <name>='<statement>' -- inline alias extension. Use uppercase name


